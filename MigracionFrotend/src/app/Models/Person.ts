
export interface IPerson {
    Id ?: Number; 
    name ?: Number; 
    lastName ?: string; 
    passport ?: string; 
    address ?: string; 
    sex ?: Number; 
    photo ?: Number; 
    birthday ?: Date; 
}